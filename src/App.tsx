import React from "react";
import styled from "styled-components";

import { Board, GitHubLink } from "components";

export const App = () => (
  <main>
    <GitHubLink />

    <Board />
  </main>
);
